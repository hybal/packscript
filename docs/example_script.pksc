use core;
set project = {
    name = "Example Project",
    id = "example",
    version = Version(1, 0, 0, 0),
    language = "packscript"
};

task "run", {
    !echo "Building "..project.name.."..."

};
